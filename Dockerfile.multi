FROM node:20.19.5-alpine3.22 AS BUILD_IMAGE

# 安装 bash
RUN  \
  sed -i 's/dl-cdn.alpinelinux.org/mirrors.ustc.edu.cn/g' /etc/apk/repositories &&\
  apk update && \
  apk add bash

WORKDIR /app

COPY .npmrc /root/
COPY package.json yarn.lock ./

RUN \
  yarn install --registry https://registry.npm.aiiko.club


COPY . .

RUN \ 
  yarn build

FROM node:20.19.5-alpine3.22

WORKDIR /

COPY --from=BUILD_IMAGE \
  /app/dist \
  /dist

RUN \
  echo "$(date +'%Y-%m-%d_%T')" > \
  "/dist/build_time_$(date +'%Y-%m-%d_%T').txt" && \
  cp -r /dist/saki-ui/ / && \
  mkdir -p /saki-ui-react && \
  cp -r /dist/react/ /saki-ui-react/components/ && \
  cp -r /dist/types/ /saki-ui-react/types/ && \
  cp -r /dist/custom-elements/ /saki-ui-custom-elements/ && \
  tar cpvzf /dist/saki-ui.tgz /saki-ui/ && \
  tar cpvzf /dist/saki-ui-react.tgz /saki-ui-react && \
  tar cpvzf /dist/saki-ui-custom-elements.tgz /saki-ui-custom-elements && \
  # yarn global add spa-http-server --registry https://registry.npmmirror.com/ && \
  rm -rf /saki-ui && \
  rm -rf /saki-ui-react && \
  rm -rf /saki-ui-custom-elements

EXPOSE 32300


CMD ["pwd"]
# CMD ["http-server","./dist","--cors","--silent","-p","23300"]

